# S14-04 — exp_123–124 : DBSCAN Monitoring by_equipment refit + accumulate

| Champ | Valeur |
|-------|--------|
| **ID** | S14-04 |
| **Sprint** | Sprint 14 |
| **Priorité** | 🔴 Critique |
| **Durée estimée** | 0.5h |
| **Dépendances** | S14-03 |
| **Fichiers cibles** | `experiments/exp_123/`, `experiments/exp_124/` |

---

## Objectif

Exécuter les deux variantes CL de `DBSCANDetector` sur Monitoring by_equipment et enregistrer les résultats dans `experiments/`.

---

## Expériences

### exp_123 — DBSCAN Monitoring by_equipment refit

```bash
python scripts/run_anomaly_detection.py \
    --model dbscan \
    --dataset monitoring \
    --scenario by_equipment \
    --strategy refit \
    --config configs/unsupervised_config.yaml \
    --exp_id exp_123
```

### exp_124 — DBSCAN Monitoring by_equipment accumulate

```bash
python scripts/run_anomaly_detection.py \
    --model dbscan \
    --dataset monitoring \
    --scenario by_equipment \
    --strategy accumulate \
    --config configs/unsupervised_config.yaml \
    --exp_id exp_124
```

---

## Sorties attendues

Chaque répertoire `experiments/exp_12X/` doit contenir :

```
experiments/exp_123/
├── config_snapshot.yaml      # copie de la config utilisée
└── results/
    └── metrics_anomaly.json  # AUROC par tâche + RAM peak
```

### Format `metrics_anomaly.json`

```json
{
  "model": "dbscan",
  "dataset": "monitoring",
  "scenario": "by_equipment",
  "strategy": "refit",
  "auroc_per_task": [0.0, 0.0, 0.0],
  "auroc_mean": 0.0,
  "avg_forgetting": 0.0,
  "ram_peak_bytes": 0,
  "inference_latency_ms": 0.0
}
```

---

## Critères d'acceptation

- [ ] `experiments/exp_123/results/metrics_anomaly.json` présent et valide
- [ ] `experiments/exp_124/results/metrics_anomaly.json` présent et valide
- [ ] `config_snapshot.yaml` présent dans les deux répertoires
- [ ] `auroc_mean` > 0.5 (DBSCAN devrait dépasser le hasard sur Monitoring)
- [ ] `ram_peak_bytes` mesuré et reporté

## Statut

⬜ À faire
