#pragma once
/* mahalanobis_int8_weights.h — GÉNÉRÉ par export_weights_c.py --maha-int8 (S2912).
 * NE PAS MODIFIER À LA MAIN (règle CLAUDE.md).
 *
 * Vide par défaut → MAHA_INT8_WEIGHTS_PROVIDED non défini → pipeline.c retombe sur
 * l'init neutre (identité), 0 régression. Régénéré par l'export avec les vrais poids
 * quantifiés (mu INT8 affine + sigma_inv INT8 affine). */
#include <stdint.h>
